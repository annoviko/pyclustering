dist:       trusty

language:   python

os:
  - linux



addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-5
    - gcc-5
    - python3

install:
  - sudo apt-get update
  - chmod u+x ci/travis-ci.sh

env:
  - CI_JOB=BUILD_CCORE
  - CI_JOB=UT_CCORE
  - CI_JOB=VALGRIND_CCORE
  - CI_JOB=UT_PYCLUSTERING
  - CI_JOB=DOCUMENTATION

matrix:
  include:
    - os:  osx
      osx_image: xcode9
      env: CI_JOB=BUILD_CCORE

script:
  - ./ci/travis-ci.sh
